add gitlab user in k8s cluster and get her token

set this vars into gitlab secret :

SERVER                              # cluster api server url
CERTIFICATE_AUTHORITY_DATA          # cluster ca.crt value
USER_TOKEN                          # gitlab user token for login